{
  "name": "dsa-allowances",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ picktd/dsa-allowances",
    "create-local": "graph create --node http://localhost:8020/ picktd/dsa-allowances",
    "remove-local": "graph remove --node http://localhost:8020/ picktd/dsa-allowances",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 picktd/dsa-allowances",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
